import { IUser, UserModel } from "../models/User.model";

const insertUser = async (user: IUser) => {
  //function to insertUser
};

const getUserById = async (userId: string) => {
  //get user by id
};
